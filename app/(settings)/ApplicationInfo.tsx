import { View, Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function ApplicationInfo() {
    return <SafeAreaView>
        <Text>
            This is a account information page..
        </Text>
    </SafeAreaView>
}